# PINNsFormer 复现计划

## 环境信息

| 项目 | 值 |
|------|-----|
| Conda 环境 | `yt_ts38` |
| Python | 3.8.20 |
| PyTorch | 1.10.1+cu111 |
| CUDA | 可用 (1 GPU) |
| 工作目录 | `/home/simplexity/cyt/pinnsformer-main` |

> **注意**：官方要求 `torch>=2.0.0`，当前环境 PyTorch 1.10.1 已验证可运行，但 LBFGS 收敛行为可能略有差异。若复现结果与论文有偏差，建议升级 PyTorch。

---

## 项目速览

```
pinnsformer-main/
├── model/
│   ├── pinnsformer.py   ← 核心模型：Transformer Encoder-Decoder + Wavelet 激活
│   ├── pinn.py           ← 基线：标准 MLP PINN (Tanh)
│   ├── qres.py           ← 基线：Quadratic Residual Network
│   └── fls.py            ← 基线：First-Layer Sine
├── util.py               ← 数据网格生成、伪时间序列、模型复制工具
├── plot_utils.py         ← 损失曲线可视化（新增）
├── demo/                 ← 四个 PDE 实验的 Jupyter Notebook
│   ├── 1d_reaction/
│   ├── convection/
│   ├── 1d_wave/
│   └── navier_stokes/
├── checkpoint/           ← 官方预训练权重 (.pt 被 gitignore 忽略)
└── paper/                ← 论文 PDF
```

---

## 论文核心思想

### 解决的问题
标准 PINN 在某些 PDE（特别是对流方程、反应方程）上存在 **failure mode**：预测结果过度平滑，无法捕捉高频/陡峭特征。

### 核心创新

1. **伪时间序列 (Pseudo Time Sequence)**：
   - 将单点输入 `[x, t]` 扩展为序列 `[x, t], [x, t+Δt], [x, t+2Δt], ...`
   - 序列长度 k=5，步长 Δt=1e-4
   - 让 Transformer 学习时间方向上的依赖关系

2. **Wavelet 激活函数**：
   - `WaveAct(x) = w₁·sin(x) + w₂·cos(x)`
   - 可学习参数 w₁, w₂
   - 替代传统 Tanh，更好地建模高频分量

3. **Transformer Encoder-Decoder 架构**：
   ```
   [x, t] → Linear Emb(d_model=32) → Encoder(N=1, heads=2) → Decoder(N=1, heads=2) → Linear Out → u(x,t)
   ```
   - d_model=32, d_hidden=512, d_ff=256
   - 参数量: ~453K

### 损失函数
```
Loss = Loss_PDE + Loss_BC + Loss_IC
```
PDE residual 通过自动微分（`torch.autograd.grad`）计算。

---

## 四个实验概览

| 序号 | 实验 | PDE | 训练迭代 | 预计时间 | 论文对应 |
|------|------|-----|---------|---------|---------|
| 1 | 1D Reaction | \(u_t = 5u(1-u)\) | 500 | ~6 min | Table 1 |
| 2 | Convection | \(u_t + 50u_x = 0\) | 500 | ~18 min | Table 1, Fig 4 |
| 3 | 1D Wave | \(u_{tt} = 4u_{xx}\) | 1000 | ~45 min | Table 2 |
| 4 | Navier-Stokes | 二维不可压缩流体 | 1000 | ~数小时 | Table 3, Fig 5 |

推荐的复现顺序即 1→2→3→4，由简到难。

---

## 实验 1：1D Reaction

**PDE**：\(u_t = 5u(1-u)\)

**精确解**：
\[
u(x,t) = \frac{h(x)e^{5t}}{h(x)e^{5t} + 1 - h(x)}, \quad h(x) = \exp\left(-\frac{(x-\pi)^2}{2(\pi/4)^2}\right)
\]

**运行步骤**：

### 1.1 激活环境
```bash
source /home/simplexity/anaconda3/etc/profile.d/conda.sh
conda activate yt_ts38
cd /home/simplexity/cyt/pinnsformer-main
```

### 1.2 启动 Jupyter
```bash
jupyter notebook
```

### 1.3 打开并运行
```
demo/1d_reaction/1d_reaction_pinnsformer.ipynb
```

所有 notebook 的 cell-0 已预置 `sys.path.insert`，在 VSCode 中无需额外配置即可运行。

### 1.4 关键代码逻辑
```python
# 1. 数据准备
res, b_left, b_right, b_upper, b_lower = get_data([0,2*pi], [0,1], 51, 51)  # 训练网格 51×51
res = make_time_sequence(res, num_step=5, step=1e-4)  # 构造伪时间序列 [N, 5, 2]

# 2. 模型构建
model = PINNsformer(d_out=1, d_hidden=512, d_model=32, N=1, heads=2)

# 3. 训练 — LBFGS 500 次迭代
#    PDE residual: u_t - 5*u*(1-u) = 0
#    BC: u(x=0,t) = u(x=2π,t)  (周期性)
#    IC: u(x,0) = h(x)

# 4. 评估 — 在 101×101 测试网格上计算 relative L1/L2 error
```

### 1.5 预期结果

训练完成后，应看到类似输出：
```
Loss Res: 0.018962, Loss_BC: 0.000001, Loss_IC: 0.178590
Train Loss: 0.197553
Training finished. Total time: 0h 5min 54s
```

### 1.6 验证 checkpoint（可选）

如需直接加载官方权重验证：
```python
model = PINNsformer(d_out=1, d_hidden=512, d_model=32, N=1, heads=2)
model.load_state_dict(torch.load('checkpoint/1dreaction_pinnsformer.pt', map_location=device))
model.to(device)
model.eval()
```

---

## 实验 2：Convection

**PDE**：\(u_t + 50u_x = 0\)（β=50 的对流方程）

**精确解**：从 `convection.mat` 读取 ground truth

**运行步骤**：

### 2.1 路径
```
demo/convection/convection_pinnsformer.ipynb
```

### 2.2 关键代码逻辑
```python
# PDE residual: u_t + 50*u_x = 0
loss_res = torch.mean((u_t + 50 * u_x) ** 2)

# BC: 周期性边界条件
loss_bc = torch.mean((pred_upper - pred_lower) ** 2)

# IC: u(x,0) = sin(x)
loss_ic = torch.mean((pred_left[:,0] - torch.sin(x_left[:,0])) ** 2)
```

### 2.3 预期结果
```
Loss Res: ~0.000000, Loss_BC: ~0.000031, Loss_IC: ~0.000005
Train Loss: ~0.000037
```

这是论文的关键实验——传统 PINN 在此问题上严重失败（预测过度平滑），PINNsFormer 应能恢复清晰的对流结构。

---

## 实验 3：1D Wave

**PDE**：\(u_{tt} = 4u_{xx}\)

**精确解**：
\[
u(x,t) = \sin(\pi x)\cos(2\pi t) + 0.5\sin(3\pi x)\cos(6\pi t)
\]

### 3.1 两个 Notebook

| Notebook | 说明 |
|----------|------|
| `1d_wave_pinnsformer.ipynb` | 基础版本，1000 次 LBFGS |
| `1d_wave_pinnsformer_ntk.ipynb` | NTK 版本，验证与 NTK 策略兼容 |

### 3.2 关键代码逻辑
```python
# PDE residual: u_tt - 4*u_xx = 0（需要二阶导数）
u_x = torch.autograd.grad(pred_res, x_res, ...)[0]
u_xx = torch.autograd.grad(u_x, x_res, ...)[0]
u_t = torch.autograd.grad(pred_res, t_res, ...)[0]
u_tt = torch.autograd.grad(u_t, t_res, ...)[0]
loss_res = torch.mean((u_tt - 4 * u_xx) ** 2)

# BC: u(0,t) = u(1,t) = 0
# IC1: u(x,0) = sin(πx) + 0.5sin(3πx)
# IC2: u_t(x,0) = 0
```

### 3.3 预期结果（基础版）
```
Loss Res: ~0.0005, Loss_IC: ~0.0018, Loss_BC: ~0.0115
Train Loss: ~0.0138
```

---

## 实验 4：Navier-Stokes

**PDE**：二维不可压缩 Navier-Stokes 方程
\[
\begin{aligned}
u_t + uu_x + vu_y + p_x - 0.01(u_{xx}+u_{yy}) &= 0 \\
v_t + uv_x + vv_y + p_y - 0.01(v_{xx}+v_{yy}) &= 0
\end{aligned}
\]

### 4.1 路径
```
demo/navier_stokes/naiver_stoke_pinnsformer.ipynb
```

### 4.2 注意修改
- Cell 1 有拼写错误 `zimport`，删除开头的 `z`，改为 `import`

### 4.3 特殊之处

- **输入维度**：`[x, y, t]`（3D 输入，不是 2D）
- **输出维度**：`[psi, p]`（d_out=2，流函数 + 压力）
- **模型内部 Linear 改为**：`nn.Linear(3, d_model)`（注意 notebook 中重新定义了整个模型类）
- **速度由自动微分得到**：`u = ∂ψ/∂y, v = -∂ψ/∂x`
- **数据来源**：`cylinder_nektar_wake.mat`（圆柱绕流仿真数据，N=5000 空间点 × T=200 时间步）
- **训练采样**：从 ~1M 点中随机采样 2500 点
- **序列步长**：`step=1e-2`（比 1D 实验大 100 倍）

### 4.4 关键代码逻辑
```python
# 模型输出
psi_and_p = model(x_train, y_train, t_train)  # [N, 5, 2]
psi = psi_and_p[:,:,0:1]
p = psi_and_p[:,:,1:2]

# 速度由流函数求导
u = d(psi)/dy
v = -d(psi)/dx

# Loss = 数据约束 + PDE residual
# 数据约束：预测 u,v 在 t=0 序列位置的值应接近真实 u_train, v_train
loss = mean((u[:,0] - u_train)^2) + mean((v[:,0] - v_train)^2) + mean(f_u^2) + mean(f_v^2)
```

### 4.5 预期结果

这个实验耗时最长（可能数小时），建议最后运行或在后台运行。完成后评估：
- 速度场 u, v 的相对 L2 误差
- 压力场 p 的相对 L2 误差
- 论文 Table 3 报告 PINNsFormer 在 p 上显著优于基线

---

## 附加功能

### 训练时间统计

每个 notebook 的训练 cell 已内置计时功能，训练结束后自动打印：
```
Training finished. Total time: 0h 22min 35s
```

### 损失曲线可视化

[`plot_utils.py`](plot_utils.py) 提供 `plot_loss()` 函数，训练完成后自动绘制 Loss vs Epoch 曲线（对应该文 Figure 11）。

**用法**（已在 `1d_reaction_fls.ipynb` 中集成）：
```python
from plot_utils import plot_loss
plot_loss(loss_track, save_path='./loss_curve.png')
```

默认标签为 `Loss_Res`, `Loss_BC`, `Loss_IC`，可通过 `labels` 参数自定义：
```python
# Navier-Stokes（有额外 loss 项时）
plot_loss(loss_track, labels=['Loss_Res', 'Loss_IC', 'Loss_BC', 'Loss_IC2'])
```

---

## 常见问题与解决方案

### Q1: VSCode 中 `ModuleNotFoundError: No module named 'util'`
**已修复**。所有 notebook 的 cell-0 已预置：
```python
import sys
sys.path.insert(0, '/home/simplexity/cyt/pinnsformer-main')
```
如果将来移动项目目录，需要更新此路径。

### Q2: 找不到 `.mat` 文件
- Convection: `demo/convection/convection.mat`
- Navier-Stokes: `demo/navier_stokes/cylinder_nektar_wake.mat`
- 从项目根目录启动 Jupyter（`cd /home/simplexity/cyt/pinnsformer-main`），或改用绝对路径。

### Q3: `nn.init.xavier_uniform` 警告
PyTorch 新版 API 改为 `nn.init.xavier_uniform_`，不影响运行。

### Q4: 结果与论文表格不完全一致
正常。原因：
- 不同 PyTorch/CUDA 版本浮点运算差异
- 随机种子虽然设置，但 cuDNN 等后端仍可能引入不确定性
- 应关注趋势：PINNsFormer 是否显著优于 PINNs/QRes/FLS 基线

### Q5: Navier-Stokes Cell 1 语法错误
Cell 1 开头 `zimport` → 改为 `import`。

### Q6: CUDA driver too old / RuntimeError
当前环境（PyTorch 1.10.1 + CUDA 11.1）已验证可用。如果切换其他环境遇到此错误，检查 PyTorch CUDA 版本与驱动兼容性。

### Q7: `.pt` 文件被 git 忽略
`.gitignore` 已配置忽略所有 `*.pt` 文件。这是预期行为，避免大文件进入版本控制。

---

## 验证清单

复现完成后，逐项检查：

- [ ] **1D Reaction**: loss 收敛到 ~0.20，预测图与 exact 接近
- [ ] **Convection**: loss 收敛到 ~0.00004，预测能捕捉对流特征（非平滑）
- [ ] **1D Wave**: loss 收敛到 ~0.014，预测波形清晰
- [ ] **1D Wave + NTK**: loss 比基础版更低（论文 Table 2）
- [ ] **Navier-Stokes**: 压力场预测与真实值接近，error 在合理范围
- [ ] **趋势确认**: PINNsFormer > PINNs/QRes/FLS 在 failure mode PDE 上
- [ ] **Loss 曲线**: 每个实验均有 loss_curve.png 输出

---

## 额外：对比基线模型

每个 demo 文件夹下有对应的基线 notebook：

| 实验 | PINN Baseline | FLS Baseline | QRes Baseline |
|------|--------------|-------------|---------------|
| 1D Reaction | `1d_reaction_pinns.ipynb` | `1d_reaction_fls.ipynb` | `1d_reaction_qres.ipynb` |
| Convection | `convection_pinns.ipynb` | `convection_fls.ipynb` | `convection_qres.ipynb` |
| Navier-Stokes | `naiver_stoke_pinn.ipynb` | `naiver_stoke_fls.ipynb` | `naiver_stoke_qres.ipynb` |

如果要完整复现论文 Table 1-3 的对比数据，需依次运行这些基线 notebook。

---

## 额外：损失景观可视化

```bash
# 先修改 vis_landscape.py 中的路径和设备号，然后：
python vis_landscape.py
```

---

**最后更新**：2026-07-03（新增：sys.path修复、训练计时、损失曲线可视化、.gitignore）
