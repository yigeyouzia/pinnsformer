# PINNsFormer 项目复现说明

本文档用于复现 `PINNsFormer: A Transformer-Based Framework For Physics-Informed Neural Networks` 项目代码，包含项目结构说明、环境配置、运行入口和常见注意事项。

## 1. 项目结构分析

项目根目录：

```text
pinnsformer-main/
├── README.md
├── requirements.txt
├── util.py
├── vis_landscape.py
├── pyhessian.py
├── model/
├── demo/
├── checkpoint/
└── paper/
```

### 1.1 根目录文件

| 文件 | 作用 |
|---|---|
| `README.md` | 官方简要说明，包含论文信息和训练入口提示 |
| `requirements.txt` | pip 依赖列表 |
| `util.py` | 数据网格生成、伪时间序列生成、模型复制等工具函数 |
| `vis_landscape.py` | 损失景观可视化脚本，需要先训练并保存模型 |
| `pyhessian.py` | Hessian / loss landscape 分析相关工具 |

### 1.2 `model/` 模型目录

```text
model/
├── pinn.py
├── pinnsformer.py
├── qres.py
└── fls.py
```

| 文件 | 模型 | 说明 |
|---|---|---|
| `pinn.py` | `PINNs` | 传统 MLP-based PINNs 基线 |
| `pinnsformer.py` | `PINNsformer` | 本文核心模型，使用伪序列、Transformer Encoder-Decoder 和 Wavelet 激活 |
| `qres.py` | `QRes` | Quadratic Residual Network 基线 |
| `fls.py` | `FLS` | First-Layer Sine 基线 |

核心模型 `PINNsformer` 的输入输出：

```python
output = model(x, t)
```

其中 1D PDE 实验中：

```text
x shape:      [N, k, 1]
t shape:      [N, k, 1]
cat(x,t):     [N, k, 2]
output shape: [N, k, 1]
```

`k` 是伪时间序列长度。测试时通常取第一个序列位置：

```python
pred = model(x_test, t_test)[:, 0:1]
```

### 1.3 `demo/` 实验目录

```text
demo/
├── convection/
├── 1d_reaction/
├── 1d_wave/
└── navier_stokes/
```

| 目录 | 实验 PDE | 主要 notebook |
|---|---|---|
| `demo/convection/` | 1D convection equation | `convection_pinnsformer.ipynb` |
| `demo/1d_reaction/` | 1D reaction equation | `1d_reaction_pinnsformer.ipynb` |
| `demo/1d_wave/` | 1D wave equation | `1d_wave_pinnsformer.ipynb`, `1d_wave_pinnsformer_ntk.ipynb` |
| `demo/navier_stokes/` | 2D Navier-Stokes equation | `naiver_stoke_pinnsformer.ipynb` |

每个 demo notebook 通常包含：

1. 导入依赖和模型。
2. 构造训练网格、边界点和初始点。
3. 使用 `make_time_sequence` 构造伪时间序列。
4. 定义 PDE residual loss、边界/初始条件 loss。
5. 使用 `LBFGS` 训练。
6. 在测试网格上预测并保存可视化结果。

### 1.4 `checkpoint/` 预训练权重目录

```text
checkpoint/
├── convection_pinnsformer.pt
├── 1dreaction_pinnsformer.pt
├── 1dwave_pinnsformer_ntk.pt
├── ns_pinnsformer.pt
├── environment.yml
└── README.md
```

该目录保存官方提供的预训练权重。`checkpoint/README.md` 说明这些 checkpoint 使用与 demo notebook 相同的超参数，但由于包版本和 CUDA 环境差异，结果可能不会完全逐数值一致，应以主要趋势一致为准。

### 1.5 `paper/` 论文目录

```text
paper/
├── PINNsFormer.pdf
└── reader_PINNsFormer/
```

| 路径 | 说明 |
|---|---|
| `PINNsFormer.pdf` | 原论文 PDF |
| `reader_PINNsFormer/paper.md` | 已生成的中英对照阅读稿 |
| `reader_PINNsFormer/source_map.json` | 阅读稿 source map |
| `reader_PINNsFormer/assets/` | 图表裁剪 |

## 2. 如何运行项目

### 2.1 进入项目目录

```bash
cd /Users/cyt/workspace/python/Aerodynamics2/methods/pinnsformer-main
```

### 2.2 创建 Python 环境

推荐使用 Python 3.10。官方 checkpoint 目录中给出的环境包括 Python 3.10.4、PyTorch 2.0.1 和 CUDA 相关依赖。

使用 conda：

```bash
conda create -n pinnsformer python=3.10 -y
conda activate pinnsformer
```

安装依赖：

```bash
pip install -r requirements.txt
pip install jupyter
```

`requirements.txt` 中主要依赖为：

```text
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
tqdm>=4.65.0
```

如果使用 GPU，请确认 PyTorch 与本机 CUDA 版本匹配。官方 checkpoint 说明中提到 CUDA 12.2，`environment.yml` 中记录了 CUDA 11.6/11.7 相关配置；实际运行时以本机 PyTorch 能否识别 GPU 为准。

检查 PyTorch 和 GPU：

```bash
python - <<'PY'
import torch
print("torch:", torch.__version__)
print("cuda available:", torch.cuda.is_available())
print("cuda devices:", torch.cuda.device_count())
PY
```

### 2.3 修改 notebook 中的 GPU 设备号

原 notebook 中常见写法类似：

```python
device = 'cuda:3'
```

如果本机只有一张 GPU，改成：

```python
device = 'cuda:0'
```

如果没有 GPU，改成：

```python
device = 'cpu'
```

注意：使用 CPU 可以跑通流程，但 PINNsFormer 训练较慢，Navier-Stokes 实验尤其耗时。

### 2.4 启动 Jupyter

在项目根目录启动：

```bash
jupyter notebook
```

或者：

```bash
jupyter lab
```

然后打开对应 notebook。

### 2.5 运行 Convection 实验

进入：

```text
demo/convection/convection_pinnsformer.ipynb
```

该实验使用：

```text
x ∈ [0, 2π]
t ∈ [0, 1]
训练网格: 51 × 51
测试网格: 101 × 101
模型: PINNsformer(d_out=1, d_hidden=512, d_model=32, N=1, heads=2)
优化器: LBFGS(line_search_fn='strong_wolfe')
```

运行全部单元后会生成预测图、真实解图和误差图，例如：

```text
convection_pinnsformer_pred.png
convection_exact.png
convection_pinnsformer_error.png
```

### 2.6 运行 1D Reaction 实验

进入：

```text
demo/1d_reaction/1d_reaction_pinnsformer.ipynb
```

主要设置：

```text
x ∈ [0, 2π]
t ∈ [0, 1]
训练网格: 51 × 51
测试网格: 101 × 101
模型输出: u_hat(x,t)
```

运行后通常会保存：

```text
1dreaction_pinnsformer.pt
1dreaction_pinnsformer_pred.png
1dreaction_exact.png
1dreaction_pinnsformer_error.png
```

### 2.7 运行 1D Wave 实验

基础版本：

```text
demo/1d_wave/1d_wave_pinnsformer.ipynb
```

NTK 版本：

```text
demo/1d_wave/1d_wave_pinnsformer_ntk.ipynb
```

主要设置：

```text
x ∈ [0, 1]
t ∈ [0, 1]
训练网格: 51 × 51
测试网格: 101 × 101
PDE residual: u_tt - beta * u_xx
```

基础版本保存：

```text
1dwave_pinnsformer.pt
1dwave_pinnsformer_pred.png
1dwave_exact.png
1dwave_pinnsformer_error.png
```

NTK 版本保存：

```text
1dwave_pinnsformer_ntk.pt
1dwave_pinnsformer_ntk_pred.png
1dwave_pinnsformer_ntk_error.png
```

### 2.8 运行 Navier-Stokes 实验

进入：

```text
demo/navier_stokes/naiver_stoke_pinnsformer.ipynb
```

该实验读取：

```text
demo/navier_stokes/cylinder_nektar_wake.mat
```

输入为二维空间加时间：

```text
[x, y, t]
```

模型输出为：

```text
[psi, p]
```

其中：

```text
psi: stream function
p: pressure
```

速度分量通过自动微分得到：

```text
u = d psi / d y
v = - d psi / d x
```

该实验会训练速度数据约束和 Navier-Stokes PDE residual，并在测试时间切片上评估压力预测。运行后通常保存：

```text
ns_pinnsformer.pt
ns_loss_pinnsformer.npy
ns_exact.png
ns_pinnsformer_pred.png
ns_pinnsformer_error.png
```

## 3. 数据集与输入输出说明

### 3.1 1D PDE 数据形态

`util.py` 中的 `get_data` 会生成规则二维时空网格：

```python
res, b_left, b_right, b_upper, b_lower = get_data(x_range, t_range, x_num, t_num)
```

输出含义：

| 变量 | shape 示例 | 含义 |
|---|---:|---|
| `res` | `[x_num * t_num, 2]` | PDE residual 采样点 |
| `b_left` | `[x_num, 2]` | 网格一侧边界 |
| `b_right` | `[x_num, 2]` | 网格另一侧边界 |
| `b_upper` | `[t_num, 2]` | 空间上边界 |
| `b_lower` | `[t_num, 2]` | 空间下边界 |

经过伪时间序列生成后：

```python
seq = make_time_sequence(res, num_step=5, step=1e-4)
```

shape 变为：

```text
[N, 5, 2]
```

每个样本类似：

```text
[
  [x, t],
  [x, t + Δt],
  [x, t + 2Δt],
  [x, t + 3Δt],
  [x, t + 4Δt]
]
```

### 3.2 1D PDE 模型输入输出

模型调用：

```python
pred = model(x, t)
```

其中：

```text
x:    [N, k, 1]
t:    [N, k, 1]
pred: [N, k, 1]
```

训练时会对整个序列算 PDE residual；测试时通常只取第一个位置：

```python
pred = model(x_test, t_test)[:, 0:1]
```

### 3.3 Navier-Stokes 数据形态

Navier-Stokes 实验从 `.mat` 文件读取仿真数据：

```python
data = scipy.io.loadmat('./cylinder_nektar_wake.mat')
```

包含空间坐标、时间、速度场和压力场。训练时采样若干点：

```text
x_train
y_train
t_train
u_train
v_train
```

PINNsFormer 输出：

```text
psi_and_p = model(x_train, y_train, t_train)
```

其中：

```text
psi = psi_and_p[:, :, 0:1]
p   = psi_and_p[:, :, 1:2]
```

再通过 `psi` 求导得到速度：

```text
u = d psi / d y
v = - d psi / d x
```

## 4. 使用 checkpoint 复现

如果只想加载预训练结果，可使用 `checkpoint/` 中的权重：

```text
checkpoint/convection_pinnsformer.pt
checkpoint/1dreaction_pinnsformer.pt
checkpoint/1dwave_pinnsformer_ntk.pt
checkpoint/ns_pinnsformer.pt
```

加载时模型结构必须与训练时一致。例如 1D PDE：

```python
from model.pinnsformer import PINNsformer

model = PINNsformer(
    d_out=1,
    d_hidden=512,
    d_model=32,
    N=1,
    heads=2,
)
model.load_state_dict(torch.load('checkpoint/convection_pinnsformer.pt', map_location=device))
model.to(device)
model.eval()
```

Navier-Stokes 的输出维度为 2：

```python
model = PINNsformer(
    d_out=2,
    d_hidden=512,
    d_model=32,
    N=1,
    heads=2,
)
```

## 5. 损失景观可视化

官方 README 提到，若要可视化 loss landscape，需要先训练并保存模型，然后运行：

```bash
python3 vis_landscape.py
```

使用前需要检查 `vis_landscape.py` 中的模型路径、数据路径和设备号是否与本地环境一致。

## 6. 常见问题

### 6.1 `cuda:3` 报错

如果报错：

```text
Invalid device ordinal
```

说明 notebook 指定了不存在的 GPU。把：

```python
device = 'cuda:3'
```

改为：

```python
device = 'cuda:0'
```

或：

```python
device = 'cpu'
```

### 6.2 找不到 `.mat` 文件

Convection 和 Navier-Stokes demo 依赖局部 `.mat` 数据：

```text
demo/convection/convection.mat
demo/navier_stokes/cylinder_nektar_wake.mat
```

请从对应 demo 目录启动 notebook，或把读取路径改成绝对路径。

### 6.3 notebook 导入 `util` 或 `model` 失败

建议从项目根目录启动 Jupyter：

```bash
cd /Users/cyt/workspace/python/Aerodynamics2/methods/pinnsformer-main
jupyter notebook
```

如果仍然失败，可在 notebook 顶部添加：

```python
import sys
sys.path.append('/Users/cyt/workspace/python/Aerodynamics2/methods/pinnsformer-main')
```

### 6.4 训练结果和论文表格不完全一致

可能原因包括：

| 原因 | 说明 |
|---|---|
| 随机种子 | notebook 中虽然设置了部分 seed，但 CUDA 和包版本仍可能带来差异 |
| PyTorch/CUDA 版本 | checkpoint README 明确说明不同环境可能无法逐数值一致 |
| GPU/CPU 差异 | 浮点计算顺序和精度会影响结果 |
| 训练轮数 | demo 中有些实验为 500 次迭代，有些为 1000 次 |

复现时应重点关注整体趋势：PINNsFormer 是否比 PINNs/QRes/FLS 更好，而不是要求每个数值完全相同。

## 7. 推荐复现顺序

建议按以下顺序运行：

1. `demo/1d_reaction/1d_reaction_pinnsformer.ipynb`
2. `demo/convection/convection_pinnsformer.ipynb`
3. `demo/1d_wave/1d_wave_pinnsformer.ipynb`
4. `demo/1d_wave/1d_wave_pinnsformer_ntk.ipynb`
5. `demo/navier_stokes/naiver_stoke_pinnsformer.ipynb`

理由：

| 顺序 | 实验 | 建议原因 |
|---:|---|---|
| 1 | 1D Reaction | 数据简单，无外部 `.mat` 依赖，适合先跑通 |
| 2 | Convection | 有 `.mat` ground truth，可复现实验主结果 |
| 3 | 1D Wave | 涉及二阶导数，适合理解 PDE residual |
| 4 | 1D Wave + NTK | 在基础 wave 实验之后理解 NTK 组合 |
| 5 | Navier-Stokes | 计算量最大，输入输出也最复杂，最后运行 |

## 8. 快速理解复现目标

复现时主要看三类输出：

| 输出 | 看什么 |
|---|---|
| loss 曲线 | 是否收敛，是否比基线更低 |
| prediction 图 | 预测场是否接近真实解 |
| error 图 | 误差是否集中降低，是否缓解 PINNs failure mode |

这套代码的核心不是训练一个普通监督模型，而是验证：

```text
把单点 [x,t] 扩展成伪时间序列后，
Transformer 是否能更好地学习 PDE 的时间依赖，
从而让 u_hat(x,t) 更符合真实解和物理约束。
```


## demo
`demo/` 下面四个文件夹分别对应论文里的四类 PDE 实验。它们不是随便取的名字，而是按 **PDE 类型 / 物理问题类型** 命名的。

| 文件夹 | 取名含义 | 实验在做什么 | 和论文关系 |
|---|---|---|---|
| `1d_reaction/` | 一维反应方程 | 模拟一个标量 `u(x,t)` 随时间发生反应增长/衰减 | 对应论文 **Table 1**，用于验证 PINNsFormer 能缓解 PINNs failure mode |
| `convection/` | 对流方程 | 模拟标量沿空间方向被输运，比如浓度/波形被平移 | 对应论文 **Table 1** 和 **Figure 4**，是主要 failure mode 实验 |
| `1d_wave/` | 一维波动方程 | 模拟波在一维空间中传播，`u(x,t)` 可理解为位移/振幅 | 对应论文 **Table 2**，用于验证 PINNsFormer 可以结合 NTK 等学习策略 |
| `navier_stokes/` | Navier-Stokes 流体方程 | 模拟二维流体速度场和压力场 | 对应论文 **Table 3** 和 **Figure 5**，用于验证高维复杂 PDE 上的泛化能力 |

**1D Reaction**

名字意思是：

```text
1D = 一维空间
Reaction = 反应过程
```

这个实验求解一维 reaction PDE。它可以直观理解为：某个物质浓度 `u(x,t)` 在空间位置 `x` 和时间 `t` 上发生反应变化。

它在论文中的作用是测试：

```text
PINNsFormer 是否能解决传统 PINNs 在简单但高变化 PDE 上的失败问题
```

论文里它和 `convection` 一起放在 **Table 1**，属于 failure mode 实验。

**Convection**

名字意思是：

```text
Convection = 对流 / 输运
```

这个实验求解一维 convection PDE。可以理解为：某个标量场 `u(x,t)` 被流动带着往前移动，比如浓度、温度扰动、密度波等。

它是论文里非常关键的实验，因为传统 PINNs 在这个问题上容易预测出过度平滑的结果，而 PINNsFormer 能明显恢复更准确的结构。

对应论文：

```text
Table 1: Convection + 1D-Reaction 结果
Figure 4: Convection 上 PINNs 和 PINNsFormer 的预测/误差对比
```

**1D Wave**

名字意思是：

```text
1D = 一维空间
Wave = 波动
```

这个实验求解一维波动方程。`u(x,t)` 可以理解为某根弦、介质或波场在位置 `x`、时间 `t` 的位移/振幅。

它在论文中的作用不是单纯证明 PINNsFormer 比 PINNs 强，而是证明：

```text
PINNsFormer 可以和已有 PINNs 优化方法结合
```

比如 notebook 里有：

```text
1d_wave_pinnsformer.ipynb
1d_wave_pinnsformer_ntk.ipynb
```

其中 `ntk` 是 Neural Tangent Kernel 方法。论文 **Table 2** 比较了：

```text
PINNs
PINNsFormer
PINNs + NTK
PINNsFormer + NTK
```

结论是 `PINNsFormer + NTK` 最好。

**Navier-Stokes**

名字意思是：

```text
Navier-Stokes = 纳维-斯托克斯方程
```

这是流体力学中非常经典的 PDE，用来描述流体运动。这个实验比前三个复杂，因为输入不是 `[x,t]`，而是：

```text
[x, y, t]
```

也就是二维空间加时间。

模型关注的物理量包括：

```text
u, v = 速度分量
p = 压力
```

代码里模型直接输出的是类似：

```text
[psi, p]
```

其中 `psi` 是流函数，再通过自动微分得到速度 `u, v`。

它在论文中的作用是验证：

```text
PINNsFormer 不只在简单 1D PDE 上有效，
在更复杂、高维的流体 PDE 上也有更好的收敛和泛化能力。
```

对应论文：

```text
Table 3: Navier-Stokes 数值结果
Figure 5: Navier-Stokes 训练 loss 对比
```

总结一下：

| 论文目的 | 对应 demo |
|---|---|
| 证明能缓解 PINNs failure mode | `convection/`, `1d_reaction/` |
| 证明能结合 NTK 等已有训练策略 | `1d_wave/` |
| 证明能处理更复杂高维 PDE | `navier_stokes/` |

所以复现时建议顺序是：

```text
1d_reaction -> convection -> 1d_wave -> navier_stokes
```

前两个最能体现论文核心问题，`1d_wave` 看方法兼容性，`navier_stokes` 看复杂流体场泛化能力。